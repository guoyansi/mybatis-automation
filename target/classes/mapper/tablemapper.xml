<?xml version="1.0" encoding="UTF-8" ?> 
<!DOCTYPE mapper  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"  
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="table.dao.ITableDao">
	<!--获取tableList  -->
	<select id="getTableList" resultType="string">
		SHOW TABLES
	</select>
	<!-- 获取表结构 -->
	<select id="getFiledList" parameterType="string" resultType="table.bean.FieldBean">
		DESC ${_parameter}
	</select>
	
</mapper>